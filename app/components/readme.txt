A home for UI building blocks

These can be lower-level generic sorts of things (like "common"), or bit specific to my UI (like "chat").
The common theme is that they're only UI. They don't have state (unless they need it for UI-specific things).
They are not wired to my data source.